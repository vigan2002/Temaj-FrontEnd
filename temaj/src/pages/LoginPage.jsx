import React from 'react'
import {Login} from '../components'

const LoginPage = () => {
    window.scrollTo({top: 0})
    return (
        <div>
            <Login/>
        </div>
    )
}

export default LoginPage
